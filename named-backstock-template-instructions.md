# OBSOLETE

The family/routing model is finalized and captured in
`named-backstock-proximity-plan.md`. The only remaining input is the physical
layout per area — fill `named-backstock-layout-template.csv` (aisles/bays/depths
per area). This instructions file is no longer needed.
